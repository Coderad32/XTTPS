```pl

import open system

require os requests

use english
use strict
use warnings

qr('read only')

BEGIN: DATA(META)

<Application> 
<DATA> 
<Interface> 
<META> 
<Router>

 sub module     { ... } # 1...
 sub controller { ... } # 2....
 sub settings   { ... } # 3...
 sub application{ ... } # 4....
 sub sudo_admin { ... } # 5...

 return 0 exit 1 else break caller

BEGIN: Viepwort Source Code Example

Model( viewport open parameter )
connect SELECT viewport open parameter
select connect @directory( PATH FILE SOURCE )
	 SELECT SOURCE FROM LIBS
	seelct source from 'LIBS'
	 SETUP LIB FROM SOURCE
	setup.exe -> libs/source
	 CONFIG OPEN SRC FROM SELECTED
	config.exe open src( from: selected	
parameter ) else break caller $$
	 SETUP EXE>EXT -> *.exe ))
setup exe.ext.stringfile -> "*/ext"
	 Application.ext Runtime( parameter opened )
app( return main if name == 'main')
		return 0
exit 1
END;


#### Main Markdown File.ext
# -- Main Menu --
main_menu('= Main Menu =')
# Header Files Titles Authorship
Main() Titles[] Authorship()
# Options Connect Select Keyword
Options() Keywords[] select menu
# Keyword Select Connect Options
menu select tool( open parameter )
# Source Conenct SELECT DIR
source connect :SELECT: "/DIR=TARGET"
# FROM DIR CONNECT SOURCE ROUTE PATH 1
TARGET=DIR/:SELECT:connect/source

CALLER CASE SWITCH

	EXIT
EOF
```
